{
  "name": "my-pkg",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && rollup -c",
    "build-no-plugins": "rimraf dist && rollup --config rollup.no-plugins.config.js"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^15.0.0",
    "@rollup/plugin-node-resolve": "^9.0.0",
    "rimraf": "^3.0.2",
    "rollup": "^2.26.11"
  }
}
